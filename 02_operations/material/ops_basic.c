#include <stdio.h>
#include <limits.h>

int main(void)
{
    /*
        Arithmetic Operators:
          +   : Addition
          -   : Subtraction
          *   : Multiplication
          /   : Division
          %   : Modulus
          ++  : Increment
          --  : Decrement
    */

    int a = 4;
    int b = 8;
    // printf("%d + %d = %d\n", a, b, a + b);

    // // Beachte Maximale Größe des Dateityps!
    // int maxInt = INT_MAX;
    // printf("%d + 1 = %d\n", maxInt, maxInt + 1);

    // int minInt = INT_MIN;
    // printf("%d + 1 = %d\n", minInt, minInt - 1);

    // printf("%d * %d = %d\n", a, b, a * b);

    // // Division
    // printf("%d / %d = %d\n",  a, b, a / b);

    // printf("%d / %d = %f\n",  a, b, a / b);
    
    // float a_float = (float) a;
    // float b_float = (float) b;
    // printf("%f / %f = %f\n", a_float, b_float, a_float / b_float);


    // // Modulo
    // printf("%d %% %d = %d\n", a, b, a % b);
    

    // // Inkrement ++ bedeutet += 1
    // printf("%d ", a++);
    // printf("%d ", a);

    // printf("%d ", --a);
    // printf("%d\n", a);


    /*
        NOTE:
         a  = 5 : -> a has value 5
         a += 1 : same as a = a + 1
         a -= 1 : same as b = b - 1
        usw...
    */

    return 0;
}